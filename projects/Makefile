clean:
	(cd actix-web && make clean)
	(cd closures && make clean)
	(cd digits_sum_has_nth_roots && cargo clean)
	(cd errors && cargo clean)
	(cd factorial && cargo clean)
	(cd fs && make clean)
	(cd generics/point && cargo clean)
	(cd hello_calc && cargo clean)
	(cd hello_world && cargo clean)
	(cd interior-mutability && cargo clean)
	(cd iterator/iterator-vec-demo && cargo clean)
	(cd lifetime && make clean)
	(cd loop-1million && make clean)
	(cd math && make clean)
	(cd operator-overloading && make clean)
	(cd option-take && cargo clean)
	(cd ownership && cargo clean)
	(cd pattern-syntax && cargo clean)
	(cd singly-linked-list && cargo clean)
	(cd threads && make clean)
